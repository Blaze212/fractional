import { SectionHeading } from 'bh-systems-ui';

export const WithDescription = () => (
  <SectionHeading
    eyebrow="What I do"
    title="Start with one workflow. Own the whole system."
    description="I walk into systems that kind of work and make them survive production and scale."
  />
);

export const Centered = () => (
  <SectionHeading centered eyebrow="Let's talk" title="Who's watching your AI when it breaks?" />
);
